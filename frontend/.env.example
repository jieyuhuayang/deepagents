# 前端默认配置（构建期注入）
#
# 拷贝为 .env.local 并改成自己环境的值，然后 `next build && next start`。
# 用户首次访问会用这些默认值跳过配置弹窗；点"设置"覆盖的值仍写入 localStorage。
#
# NEXT_PUBLIC_* 是构建期内联到 bundle 的，改完必须重新 build。

# Langgraph backend 地址。两种填法：
#   1) 相对路径 `/api/langgraph`（推荐，公网/局域网/任何 origin 都通用）
#      —— Next.js rewrites（next.config.ts）会反向代理到本机 backend；
#      —— 访客浏览器无需直连 backend 端口，只跟前端同 origin。
#   2) 绝对 URL 如 `http://192.168.106.114:12024`
#      —— 只适合所有访客都能直连这个地址的场景（同局域网/VPN）。
NEXT_PUBLIC_DEPLOYMENT_URL=/api/langgraph

# Graph 名（CLAUDE.md 强约束：必须是 langgraph.json 里的 graph 名）
NEXT_PUBLIC_ASSISTANT_ID=research

# 可选：LangSmith API Key
# NEXT_PUBLIC_LANGSMITH_API_KEY=

# 可选：next.config.ts rewrites 的反代目标（默认 http://127.0.0.1:12024）
# LANGGRAPH_INTERNAL_URL=http://127.0.0.1:12024
